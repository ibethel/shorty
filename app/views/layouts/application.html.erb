<!DOCTYPE html>
<html>
<head>
	<title><%= @page_title || "Shorten that URL" %> | bcrr.us</title>
	<%= stylesheet_link_tag "reset", "grid", "tabs", "autocomplete", "stylesheet", cache: "styles" %>
	<%= javascript_include_tag "jquery", "jquery.tabs", "rails", "autocomplete-rails", "application", cache: "scripts" %>
	<%= csrf_meta_tag %>
</head>
<body id="<%= controller.controller_name %>">

	<div class="container">
		<header>
			<div class="span-5" id="logo">
				<%= link_to(image_tag("logo.png"), admin_root_path)%>
			</div>
			<nav class="span-19 last">
				<ul>
					<% if controller.controller_name != "embedded_forms" %>
						<li><%= link_to "Embedded Forms", admin_embedded_forms_path %></li>
					<% end %>

					<% if current_user.present? %>
						<li><%= link_to "My Account", admin_users_path %></li>
						<li><%= link_to "Site Keys", new_admin_user_path %></li>
						<li><%= link_to "Sign Out", signout_path %></li>
					<% else %>
						<li>No User!</li>
					<% end %>
				</ul>
			</nav>
			<div class="clear"></div>
		</header>

		<section id="flash-messages">
			<% if notice %>
			<p class="notice"><%= notice %></p>
			<% end %>

			<% if alert %>
			<p class="alert"><%= alert %></p>
			<% else %>
			<p class = "alert"><%= "We are updating this site! Due to these changes the url to manage your short links will be changing. Starting June 15, 2015 to access bcrr.us please go to http://bcrr.us/admin/" %></p>
			<% end %>
		</section>

		<section id="above-wrapper">
			<%= yield :above_wrapper %>
		</section>

		<div id="wrapper" class="clear span-22 append-1 prepend-1 last">
			<%= yield %>
		</div>

		<footer class="clear span-24 last">

		</footer>
	</div>

</body>
</html>
